/// This file contains the `tick` function, which is called on every timer interrupt.
/// Literally an entire file for one function. :)

/// This function is called on every timer interrupt.
pub fn tick() {}
